extends layout

block content
  section.about
    .container
      .row.mx-xl-5
        .col-6.back__subtitle-top
          a.back__subtitle-root(href='https://centervnl.ru/about/') О нас
          svg.news__link-image(width='20' height='5' viewBox='0 0 20 5' fill='none' xmlns='http://www.w3.org/2000/svg')
            path.news__link-image(fill-rule='evenodd' clip-rule='evenodd' d='M17.1044 5L19.6044 2.5L17.1044 0V2H0V3H17.1044V5Z' fill='#656973')
          a.back__subtitle-child(href='https://centervnl.ru/projects/') 
            |  Гранты и субсидии

        .col-6.back__subtitle-top.text-end 
          +share("/projects/"+project.path, project.name)

        h1.col-12.header__subtitle.title= project.name
        .col-12.grants__date.mt-2 #{project.date}

    - gallery = (project.gallery && project.gallery.length) ? [...project.gallery] : []
    if project.image
      - gallery.unshift(project.image)
    if gallery.length
      .container.mt-5
        +slider(gallery)

    .container
      .row.mx-xl-5
        .mt-5
          +long-text(project.description)

    if project.video
      .container
        .row.mx-xl-5
          h2.col-12.header__subtitle.subtitle Видео о проекте
        .row.mx-xl-5.m-3
          .col-12.mt-3
            +video(project.video)

    .container
      .row.mx-xl-5        
        .mt-3 
          +long-text('Если у вас возникли вопросы, вы можете задать их нам в разделе контакты.')

        .mt-4
          a.button.button_orange(href="https://centervnl.ru/contacts#form") Задать вопрос

    if project.content && project.content.length
      .container
        .row.mx-xl-5
          h2.col-12.header__subtitle.subtitle Документы проекта
        .row.mx-xl-5.mt-3
          each item in project.content 
            .col-12.col-md-6.col-lg-4.mt-3 
              .row.d-flex.align-items-center
                a.doc-icon.d-inline-block(href= item.url target="_blank")
                a.news__subtitle.col.ms-2(href= item.url target="_blank")= item.text || 'Документ'




extends layout

block content
  section.about
    .container
      .row.mx-xl-5
        .col-12
          a.back__subtitle.back__subtitle-root(href='/about/') О нас
          svg.news__link-image(width='20' height='5' viewBox='0 0 20 5' fill='none' xmlns='http://www.w3.org/2000/svg')
            path.news__link-image(fill-rule='evenodd' clip-rule='evenodd' d='M17.1044 5L19.6044 2.5L17.1044 0V2H0V3H17.1044V5Z' fill='#656973')
          a.back__subtitle(href='/projects/') 
            |  Гранты и субсидии
        .col-12.header__subtitle.title= project.name
        .col-12.grants__date.mt-2 #{project.date}

    if project.image
      .container.mt-5
        img.background__image(src= project.image alt='bg')

    .container
      .row.mx-xl-5
        .mt-5
          +long-text(project.description)



  //- if project.description
  //-   hr
  //-   .text-justify.col-12 !{project.description}
  //- if (project.gallery && project.gallery.length) || project.image
  //-   hr
  //-   - var gallery= []
  //-   if project.gallery && project.gallery.length
  //-     - gallery = [...project.gallery]
  //-   if project.image
  //-     - gallery.push(project.image)
  //-   - var sliderId= 'ID'+project._id
  //-   include includes/blur-slider



